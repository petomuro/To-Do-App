# To-Do-App

To-Do-App - Simple To Do App created in Vue 3 + TypeScript + Vite and Mockapi.

## Project setup

Firstly install all necessary modules with:

`npm install`

and then run:

`npm run dev`

## Project build

`npm run build`
